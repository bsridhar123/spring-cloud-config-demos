spring:
  application:
    name: configuration-service
  cloud:
    bus:
      enabled: true
    config:
      server:
        native:
          search-locations: file:/home/volivattamjaadu/tracing-demo-repo/config-repo

  profiles:
    active: native
